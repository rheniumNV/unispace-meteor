{
	"name": "@unispace-meteor/data-client",
	"version": "1.0.0",
	"description": "外部データ取得・変換ライブラリ",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"keywords": [],
	"author": "LinaTsukusu",
	"license": "ISC",
	"scripts": {
		"build": "tsc",
		"dev": "tsc-watch",
		"clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
		"lint": "biome check src",
		"lint:fix": "biome check --write src",
		"format": "biome format --write src",
		"test": "vitest run",
		"test:watch": "vitest watch",
		"test:ui": "vitest --ui",
		"test:coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"vitest": "3.2.4"
	}
}
