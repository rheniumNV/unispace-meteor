{
  "name": "@unispace-meteor/common",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc-watch",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint --fix src",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "brson.js": "^1.0.5",
    "uuid": "^10.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.json": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0"
  }
}